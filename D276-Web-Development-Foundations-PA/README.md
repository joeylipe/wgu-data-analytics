# WGU D276 Web Development Foundations Performance Assessment

## Status

**This repository is stored PRIVATELY to comply with WGU Academic Authenticity guidelines and institutional integrity policies.**

---

## Project Objective

The goal was to create a personal portfolio website demonstrating proficiency in core front-end languages and problem-solving, without the use of frameworks.

### Files Included:
* 3 HTML pages: `resume.html`, `cover-letter.html`, `career-goals.html`
* 1 external CSS file (linked to all pages)
* Required media files (1 image, 1 audio, 1 video)

---

## Component Checklist

This project demonstrates compliance with the following technical requirements across all files:

### HTML Structure and Content:
* **Semantic Organization:** Utilized `<header>`, `<nav>`, `<main>`, `<section>`, and `<footer>` elements (on all pages).
* **Tables:** Implemented required table structure with a caption on `resume.html`.
* **JavaScript:** Successfully implemented **JavaScript to populate dynamic content** (current date) and write a message to the console on `resume.html`.
* **Form:** Implemented a functional **contact form** on the `career-goals.html` page featuring all required input types (email, tel, radio buttons, select option).

### CSS Implementation:
* **External Stylesheet:** Ensured one external CSS file is linked to all three HTML pages.
* **Selector Mastery:** Applied all required selectors, including **element, class, ID, descendant, and pseudo-class selectors.**
